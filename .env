# Frontend configuration - points to the backend email service
VITE_BACKEND_API_URL=http://localhost:3001

# Note: The actual SMTP configuration is in the backend .env file at /backend/.env
# This file is only for frontend environment variables
